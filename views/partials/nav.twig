<nav class="navigation">
  <figure class="hamburger" id="hamburger">
    <span class="hamburger__line"></span>
    <span class="hamburger__line"></span>
    <span class="hamburger__line"></span>
  </figure>

    <ul class="mainNav {{ fn('is_front_page') ? 'u-lg-dNone' : '' }} ">
      {% for item in nav.get_items %}
        <li class="mainNav__item {{ item.classes|join(' ') }} {{ (fn('is_front_page') ? 'u-lg-hidden' : '') }}">
          <a class="mainNav__link" href="{{ item.link }}">{{ item.title }}</a>
        </li>
      {% endfor %}
      <section class="mobileMenuFooter">{% include 'partials/footer.twig' %}</section>
    </ul>


  <a href="https://www.facebook.com/jensenblikk"><img class="headerFacebook" src="{{ facebook_icon }}" alt="facebook logo"></a>
</nav>